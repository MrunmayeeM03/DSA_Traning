rec={}
n=int(input("Enter the number of students:"))
for i in range(n):
    name=input("Enter name: ")
    per=float(input("enter perc:"))
    rec[name]=per
    print(rec)
    for x in rect:

        print(x,"\t",rec[x])
  #del
  #clear
  #len()
  #get(key)
